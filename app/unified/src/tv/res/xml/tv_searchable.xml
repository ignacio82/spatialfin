<?xml version="1.0" encoding="utf-8"?>
<!--
  Google TV global search configuration. Attached to UnifiedMainActivity via a
  <meta-data android:name="android.app.searchable"> entry in the TV-flavor
  manifest overlay.

  `searchSuggestAuthority` must match the runtime authority of
  SpatialFinSearchProvider, which in turn is `${applicationId}.search`. The
  value is supplied via `resValue("string", "search_authority", ...)` in
  app/unified/build.gradle.kts so debug/staging installs get distinct
  authorities and can coexist with release on the same device.
-->
<searchable xmlns:android="http://schemas.android.com/apk/res/android"
    android:label="@string/app_name"
    android:hint="@string/tv_search_hint"
    android:includeInGlobalSearch="true"
    android:searchSettingsDescription="@string/app_name"
    android:searchSuggestAuthority="@string/search_authority"
    android:searchSuggestIntentAction="android.intent.action.VIEW"
    android:searchSuggestThreshold="2" />
